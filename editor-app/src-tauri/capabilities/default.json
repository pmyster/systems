{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: native dialogs only. File I/O goes through custom Rust commands (read_unit_file / write_unit_file) that use std::fs directly, so we don't need any fs-plugin scope here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
