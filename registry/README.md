# Trial registry (placeholder)

Append-only log of **every** evaluated configuration — including abandoned
runs — per spec §5.1 and golden rule 6. The registry implementation lands
with the evaluation module (milestone 3); this directory exists now so the
layout is stable. Nothing may ever rewrite history in here.
